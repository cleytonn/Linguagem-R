#Aula 3 - Workspace (Básico)

#Lista os(as) objetos/variáveis criadas
ls() 

#remove os(as) objetos/variáveis criadas
rm(nomeDaVariavel) 

#Remove todos(as) objetos/variaveis criadas 
rm(list = ls())

#Criando outra variável

variavelWorkspace <- "Exemplo" 
variavelWorkspace

#Remove a variavelWorkspace
rm(variavelWorkspace) 



